//! nfwm-app: Executable composition, startup, logging, and action dispatch.

use std::process;

fn main() {
    println!("nfwm v0.1.0 — starting up...");
    // TODO: Initialize logging, parse CLI, start workspace
    println!("nfwm exiting cleanly.");
    process::exit(0);
}
